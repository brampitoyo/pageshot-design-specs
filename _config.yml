# Site settings
name: PageShot Design Specs
title: PageShot Design Specs
email: bramp@mozilla.com
description: "A design specs for PageShot"
# these need to be changed for each project
baseurl: "/pageshot-design-specs" #this is the github project name WITHOUT the trailing /
url: "https://brampitoyo.github.io"  #this the url of the GHPages account your project is hosted at

# Build settings
markdown: kramdown
permalinks: pretty

# YAML Frontmatter defaults 
# currently commented out to not break the testbed. Uncomment when the default spec template is complete
#defaults:
#-
#  scope:
#    path: '' #left empty for all files 
#  values:
#    layout: "spec-default"